import { DirectionalLight } from "three";

const state = {
  // CANVAS: {
    renderer: {
      setClearColor: {
        color: 0x0e1111,
        alpha: 1
      }, 
    },
    scene: {
        // camera: DEFAULT_CAMERA
        // lights: 
        // lights: {  directional: new DirectionalLight() }
    }
  // }
}
export { state }