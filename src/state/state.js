const state = {
  // CANVAS: {
    renderer: {
      setClearColor: {
        color: 0x0e1111,
        alpha: 1
      }, 
      scene: {
        // camera: DEFAULT_CAMERA
      }
    }
  // }
}
export { state }